export const implicitDiffDocs = {
  name: 'implicitDiff',
  category: 'Algebra',
  syntax: [
    'implicitDiff(expr, y, x)'
  ],
  description: 'Compute the implicit derivative dy/dx of an equation F(x, y) = 0 using the formula dy/dx = -(dF/dx) / (dF/dy).',
  examples: [],
  seealso: [
    'derivative', 'partialDerivative', 'simplify'
  ]
}
